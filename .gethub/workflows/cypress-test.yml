name: Cypress tests demo
on: [push]
jobs: 
    cypress-run:
      run-on: ubuntu-latest
      steps:
         -name: Checkout
          uses: actions/checkout@v3
         -name: Cypress run
          uses: cypress-io/github-action@v5